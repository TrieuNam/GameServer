server:
  port: 8080

spring:
  data:
    redis:
      host: localhost  # hoặc "redis" nếu dùng docker-compose
      port: 6379
  datasource:
    url: jdbc:mysql://localhost:3309/session_db
    username: tpnam
    password: 121831
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetchRegistry: true
    registerWithEureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/