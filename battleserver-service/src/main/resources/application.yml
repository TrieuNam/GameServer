server:
  port: 8082

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetchRegistry: true
    registerWithEureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: battle-service
  datasource:
    url: jdbc:mysql://localhost:3328/db_bag_service?useUnicode=true&characterEncoding=UTF-8
    username: tpnam
    password: 121831
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379


