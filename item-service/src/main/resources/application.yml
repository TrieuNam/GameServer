server:
  port: 8102


spring:
  application:
    name: item-service
  threads:
    virtual:
      enabled: true
  datasource:
    url: jdbc:mysql://localhost:3316/db_item_service?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=UTC
    username: tpnam
    password: 121831
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

logging:
  level:
    com.example.item: INFO