spring:
  application:
    name: main-fb-service
  threads:
    virtual:
      enabled: true   # Java 21 Virtual Threads
  datasource:
    $133093/game_mainfb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: tpnam
    password: 121831
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  flyway:
    enabled: true
    baseline-on-migrate: true
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8128

grpc:
  server:
    port: 9096
    max-inbound-message-size: 4MB

eureka:
  instance:
    prefer-ip-address: true
    metadata-map:
      grpc.port: 9096
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

logging:
  level:
    com.SouthMillion: DEBUG

mainfb:
  stamina-item-id: 50001
