package com.SouthMillion.lingzhu_service;

import org.junit.jupiter.api.Test;

/**
 * Smoke test – verifies the application context loads without errors.
 * Kept as a plain unit test (no @SpringBootTest) to avoid requiring a DB.
 */
class LingZhuServiceApplicationTests {

    @Test
    void contextLoads() {
        // Passes if compilation + class-loading succeed
    }
}

