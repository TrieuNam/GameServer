spring:
  application:
    name: globalserver-service
  datasource:
    url: jdbc:mysql://localhost:3317/globalserver_service_db?useUnicode=true&characterEncoding=UTF-8
    username: tpnam
    password: 121831
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379

  kafka:
    bootstrap-servers: localhost:9092

server:
  port: 8100


eureka:
  instance:
    prefer-ip-address: true
  client:
    fetchRegistry: true
    registerWithEureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

